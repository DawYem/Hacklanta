# Copy to .env.
# Server-only (not bundled):
MAPS_PLATFORM_API_KEY=
# Maps JavaScript API — required for the live map in the app. Restrict by HTTP referrer in Google Cloud.
VITE_MAPS_PLATFORM_API_KEY=
